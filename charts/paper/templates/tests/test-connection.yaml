apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paper.fullname" . }}-test-connection"
  labels:
    {{- include "paper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
